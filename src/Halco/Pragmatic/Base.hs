module Halco.Pragmatic.Base where

-- import           Data.Map          (Map)
-- import qualified Data.Map          as Map
-- import           Data.Set          (Set)
-- import qualified Data.Set          as Set

-- import qualified Halco.Conts.Types as Types
-- import qualified Halco.Defs        as Defs

-- type AttrName = String
-- newtype Attr = Attr AttrName deriving (Show, Ord, Eq)
-- instance Defs.Attr Attr

-- type PropName = String
-- newtype Prop = Prop PropName deriving (Show, Ord, Eq)
-- instance Defs.Prop Attr

-- type AttrProps = Prop
-- data AttrRelation = AttrRelation PropName (Attr, Attr)
--   deriving (Show, Ord, Eq)

-- data InCont = InCont
--   { attrProps :: Map Attr AttrProps
--   , relProps  :: Set AttrRelation }
--   deriving (Show)

-- data OutCont = OutCont
--   {}
--   { attrsO :: () -- TODO
--   , propsO :: () -- TODO
--   }
--   deriving (Show)
